<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <%= link_to 'Rsyslog Analyzer', root_path, class: 'navbar-brand' %>
  </div>

  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li><%= link_to("Sign out", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure to sign out?" }, title: 'Sign out') %></li>
    <% end %>
  </ul>
</nav>
